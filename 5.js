
  let arr = [1, 3, 10, 0, 34, 85, 0, 12, 108];

  console.log(arr.length);
  
  let result = arr.map(function(item, index, array) {
    console.log(item);
  
    // возвращается новое значение вместо элемента
  
  });